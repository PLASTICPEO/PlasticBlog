import { Button } from "antd";

const Footer = () => {
  return (
    <div className="flex items-center justify-between">
      <Button className="my-2 text-[black]">login</Button>
      <Button className="my-2">login 2</Button>
    </div>
  );
};

export default Footer;
